pipeline
        {
            agent { dockerfile true }
            environment {
                CI = 'true'
            }
            stages {
                stage('Build') {
                    steps {
                        sh 'npm install'
                    }
                }
                stage('Test') {
                    steps {
                        sh 'pwd'
                        sh '/pipeline/scripts/run-tests.sh'
                        sh 'chmod +x ./pipeline/scripts/run-tests.sh'
                        sh '/pipeline/scripts/run-tests.sh'
                    }
                }
            }
            post {
                always {
                    junit '/reports/*.xml'
                }
            }
        }
