pipeline
        {
            agent { dockerfile true }
            environment {
                CI = 'true'
            }
            stages {
                stage('Build') {
                    steps {
                        sh 'npm install'
                    }
                }
                stage('Test') {
                    steps {
                        sh 'pwd'
                        sh 'ls /pipeline/scripts'
                        sh 'chmod +x ./pipeline/scripts/run-tests.sh'
                        sh 'pwd/pipeline/scripts/run-tests.sh'
                    }
                }
            }
            post {
                always {
                    junit '/reports/*.xml'
                }
            }
        }
